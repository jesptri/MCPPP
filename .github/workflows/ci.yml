name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short

  docker:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t strava-mcp:${{ github.sha }} .

      - name: Verify container starts
        run: |
          docker run -d --name mcp-test \
            -e STRAVA_CLIENT_ID=test \
            -e STRAVA_CLIENT_SECRET=test \
            -e STRAVA_ACCESS_TOKEN=test \
            -e STRAVA_REFRESH_TOKEN=test \
            -e STRAVA_EXPIRES_AT=0 \
            -p 8000:8000 \
            strava-mcp:${{ github.sha }}
          sleep 3
          curl -sf http://localhost:8000/ | grep -q '"protocol"'
          docker stop mcp-test
